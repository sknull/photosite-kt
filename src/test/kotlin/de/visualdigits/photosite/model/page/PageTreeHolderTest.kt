package de.visualdigits.photosite.model.page

import org.junit.jupiter.api.Test

class PageTreeHolderTest {

    @Test
    fun testPageTree() {
// todo adopt to changes
//        val siteConfigHolder = Photosite()
//        val rootFolder = "E:/Programmierung/www/webserver/www/website"
//        siteConfigHolder.rootDirectory = "file:$rootFolder"
//        siteConfigHolder.siteConfig = SiteConfig.load(Paths.get(rootFolder, "resources", "config.xml").toFile())
//        siteConfigHolder.siteConfig?.photositerootFolder = rootFolder
//        siteConfigHolder.siteUrl = siteConfigHolder.siteConfig?.photositeprotocol + siteConfigHolder.siteConfig?.photositedomain
//        siteConfigHolder.pageDirectory = siteConfigHolder.siteConfig?.photositerootFolder?.let { Paths.get(it, "resources", "pagetree").toFile() }
//        siteConfigHolder.reloadPageTree()
//
//        val pages = siteConfigHolder.pageTree?.getSubTree("pagetree/Arts/Manikin Records")
//        println(pages)
    }
}
