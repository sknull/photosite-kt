security:
  require-ssl: false

spring:
  config:
    import: "file:///${user.home}/.photosite/secrets/secrets.yml"
  http:
    converters:
      preferred-json-mapper: kotlinx_serialization
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
  datasource:
    url: jdbc:sqlite:photosite.db
    driver-class-name: org.sqlite.JDBC
    username: sa
    password: ""
